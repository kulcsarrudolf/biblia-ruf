const { getBiblePassage } = require("./src/");
const { showBibleBooks } = require("./src/");
const { help } = require("./src/help");

module.exports = {
  getBiblePassage,
  showBibleBooks,
  help,
};
