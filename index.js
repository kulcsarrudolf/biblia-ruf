const { getBiblePassage } = require("./src/");
const {
  getBibleBooks,
  getBibleBooksOldTestament,
  getBibleBooksNewTestament,
} = require("./src/");

module.exports = {
  getBiblePassage,
  getBibleBooks,
  getBibleBooksOldTestament,
  getBibleBooksNewTestament,
};
