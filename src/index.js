const { getBiblePassage } = require("./passage");
const { showBibleBooks } = require("./books");
const { help } = require("./help");

module.exports = {
  getBiblePassage,
  showBibleBooks,
  help,
};
