const { getBiblePassage } = require("./passage");
const {
  getBibleBooks,
  getBibleBooksOldTestament,
  getBibleBooksNewTestament,
} = require("./books");

module.exports = {
  getBiblePassage,
  getBibleBooks,
  getBibleBooksOldTestament,
  getBibleBooksNewTestament,
};
